'use strict';

describe('myApp.game module', function() {

    beforeEach(module('myApp.game'));

    describe('GameCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var gameCtrl = $controller('GameCtrl');
            expect(gameCtrl).toBeDefined();
        }));

    });
});
